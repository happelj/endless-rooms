{
  "name": "endless-rooms",
  "version": "0.2.0",
  "private": true,
  "description": "Step 2 first-person player controller for a browser-based exploration project.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "vite": "^8.1.3"
  }
}
